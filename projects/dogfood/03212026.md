# Conversation with AI 

Me:
> When working with a bare repo and git worktrees, I've gotten things into a weird state. I'm working on/in [The GrindCLI](https://github.com/leebrandt/grind), and I noticed when I `clone`d the grind workspace, the "stars" that mark repos that are long-term in the `grind status` command weren't showing up on the new machine. So I went to the [`grind status`](https://github.com/leebrandt/grind/blob/main/src/commands/status.ts) command, and it looks as if it uses the bare repo to get the project.json to determine which are long-term. [Here](https://github.com/leebrandt/grind.dogfood) is the Grind "repo" repo :D. It's my repo where I dogfood Grind as a personal project management tool.


Turns out. I had developed thes on another box. One that died. I was probably SOOO chuffed I didn't lose anything, I forgot to run a `git -C .grind.repo.git fetch` on that bare repo. I wasn't getting any changes across machines.

Need to add a `fetch` to the bare repo after a `grind clone <repo-url>`. 
