/**
 * Trigger promo workflow for a project
 * gd promo "project"
 */
export async function promo(project: string): Promise<void> {
  console.log(`TODO: Trigger promo workflow for "${project}"`);
  console.log("  - [Future] Read project metadata");
  console.log("  - [Future] Trigger Make.com workflow");
  console.log("  - [Future] Generate social media posts");
  console.log("  - [Future] Schedule cross-platform promotion");
}
