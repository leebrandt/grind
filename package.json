{
  "name": "grind",
  "version": "0.1.0",
  "description": "CLI tool for managing creative/technical projects from idea to publication",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "gd": "./gd"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile=gd",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
